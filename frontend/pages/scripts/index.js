const html = require(`${__rootdir}/library/html.js`);

// This should contain individualized code to compile the homepage referencing whatever analytics necessary
module.exports = async function(PROCESSED_HTML){

	return PROCESSED_HTML;
}